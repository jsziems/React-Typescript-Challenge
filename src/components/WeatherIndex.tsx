import LocalWeather from "./LocalWeather"
import Location from "./Location"

const WeatherIndex=() => {
    return(
        <div>
            < LocalWeather />

        </div>
    )
}

export default WeatherIndex